version: "3.1"

services:
  e2e-runner:
    image: cypresstests
    container_name: e2e-cypress-runner
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - GITHUB_RUN_NUMBER=${GITHUB_RUN_NUMBER}
    ports:
      - "8081:8081"
    stdin_open: true  